{
  "project": "pdfplumber-rs",
  "branchName": "ralph/issue-183-cidcmap-encoding",
  "description": "Issue #183: CIDFont / CMap encoding support for multilingual PDFs",
  "relatedIssue": 183,
  "userStories": [
    {
      "id": "US-183-1",
      "title": "Implement ToUnicode CMap parser",
      "description": "Parse /ToUnicode CMap streams embedded in PDF font dictionaries. These provide direct CID → Unicode mappings using beginbfchar/endbfchar and beginbfrange/endbfrange operators. This is the highest-value CMap feature as many CIDFont PDFs include ToUnicode maps.",
      "acceptanceCriteria": [
        "ToUnicode CMap streams are parsed correctly (bfchar and bfrange entries)",
        "At least 3 previously-failing CIDFont PDFs now extract chars > 50%",
        "Handles multi-byte Unicode mappings (e.g., surrogate pairs, BMP and SMP)",
        "cargo test --workspace passes",
        "cargo clippy --workspace -- -D warnings passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "DONE (PR #193). 8 CID PDFs improved from 0% to 100%. Fixed bfchar/bfrange no-newline parsing, added cidrange Identity fallback, fixed CID Descent sign, added (cid:N) unmapped format."
    },
    {
      "id": "US-183-2",
      "title": "Implement predefined CMap support for CJK fonts",
      "description": "CIDFont dictionaries reference predefined CMaps by name (e.g., 'Adobe-Japan1-UCS2', 'GBKp-EUC-H', 'UniKS-UCS2-H'). These standard CMaps define CID → character code mappings for CJK languages. Bundle the most common predefined CMaps or implement a CMap file loader.",
      "acceptanceCriteria": [
        "At least Adobe-Japan1, Adobe-GB1, Adobe-Korea1, Adobe-CNS1 CMap families are supported",
        "Japanese PDFs (issue3521, issue7696, issue8570, issue9262) extract chars > 30%",
        "Korean PDF (pdfbox-5350-korean) extracts chars > 30%",
        "cargo test --workspace passes",
        "cargo clippy --workspace -- -D warnings passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "DONE (PR #194). Added GBKp-EUC, GBK2K, GBpc-EUC variants for Chinese Simplified. issue3521 0%→100%, pdfbox-5350-korean 100% (from US-183-1)."
    },
    {
      "id": "US-183-3",
      "title": "Implement CIDFont width handling (W/DW arrays)",
      "description": "CIDFont dictionaries specify glyph widths via /W (width array) and /DW (default width) entries. The /W array format is: [cid [w1 w2 ...]] or [cidFirst cidLast w]. Implement width extraction from these arrays for correct character positioning.",
      "acceptanceCriteria": [
        "CIDFont /W and /DW entries are parsed and used for char width calculation",
        "CIDFont PDFs have improved x-coordinate accuracy",
        "No regression on Type1/TrueType font widths",
        "cargo test --workspace passes",
        "cargo clippy --workspace -- -D warnings passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "DONE (already implemented). /W and /DW parsing in cid_font.rs parse_w_array(). CidFontMetrics::get_width() used in interpreter.rs for displacement. All CIDFont PDFs show 100% accuracy with correct word grouping. No regression on Type1/TrueType fonts."
    }
  ]
}
