# Ralph Progress Log
Started: 2026년  3월  2일 월요일 12시 15분 56초 KST

## Codebase Patterns
- `ExtractOptions` in `pdfplumber-core/src/error.rs` controls pipeline behavior. Add new options there and apply in `Pdf::page()` in `pdfplumber/src/pdf.rs`.
- Cross-validation golden data is generated from Python pdfplumber without normalization or dedup. When comparing, disable features Python doesn't apply by default.
- `Page::from_extraction()` is the internal constructor — dedup/normalization happen in `Pdf::page()` before calling it.
---

## 2026-03-02 - US-205-4
- **What was implemented:** Character deduplication enabled by default in the extraction pipeline. Added `dedupe: Option<DedupeOptions>` field to `ExtractOptions` (default: `Some(DedupeOptions::default())`). Applied dedup in `Pdf::page()` after BiDi + normalization. Updated cross-validation tests to disable dedup (matching Python golden data behavior). Un-ignored 3 dedup-related cross-validation tests.
- **Files changed:**
  - `crates/pdfplumber-core/src/error.rs` — Added `dedupe` field to `ExtractOptions`
  - `crates/pdfplumber/src/pdf.rs` — Added `dedupe_chars` import and dedup step in `Pdf::page()`
  - `crates/pdfplumber/tests/pdf_integration.rs` — Updated dedupe tests for auto-dedup behavior
  - `crates/pdfplumber/tests/cross_validation.rs` — Disabled dedup in test harness, un-ignored 3 tests
  - `crates/pdfplumber/tests/real_world_integration.rs` — Updated overlapping text tests
  - `scripts/ralph/prd.json` — Set passes: true
- **Dependencies added:** None
- **Learnings for future iterations:**
  - The `dedupe_chars` function and `DedupeOptions` already existed in `pdfplumber-core/src/dedupe.rs` with comprehensive unit tests.
  - Cross-validation golden data from Python does NOT have dedup applied. Enabling dedup by default causes score drops for PDFs with duplicates (e.g., issue-842: 430→115 chars). Solution: disable dedup in the cross-validation harness.
  - The acceptance criteria scores (76.8%, 37.3%, 46.2%) were outdated — earlier extraction bugs had been fixed in prior PRs, bringing scores to 100% before this change.
---
